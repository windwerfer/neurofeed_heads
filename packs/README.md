# Packs

One directory per shippable pack. Current:

- `cbramod-spur-a` — frozen CBraMod + A-vig heads for Muse (from `muse-eeg-heads/exports/cbramod_pack`).

Version packs with git tags: `cbramod-spur-a/v0.1.0`.
